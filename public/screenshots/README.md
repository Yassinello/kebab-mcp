# README screenshots

Hero image rendered in the project README — `dashboard.png` is a
capture of `/config` showing the unified Connectors view in light mode,
with several packs active.

When refreshing it, aim for 1280×800 or wider so GitHub's Markdown
rendering stays crisp.

Optional follow-ups:

- `welcome.png` — capture of `/welcome` showing the token-mint step.
- `install.gif` — 30-second loop of Vercel deploy → Welcome → token paste.
