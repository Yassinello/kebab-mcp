# README screenshots

Hero image rendered in the project README. Replace `dashboard.png` with a
real capture of `/config` (the unified dashboard, ideally with a few
connectors active, in light mode). 1280×800 or wider works best for
GitHub's Markdown rendering.

Optional follow-ups:

- `welcome.png` — capture of `/welcome` showing the token-mint step.
- `install.gif` — 30-second loop of Vercel deploy → Welcome → token paste.

The current `dashboard.png` is a 1×1 transparent placeholder so the
README markdown does not break before the real capture lands.
